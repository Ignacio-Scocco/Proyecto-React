
import Button from 'react-bootstrap/Button';

function CartWidget () {
    return (
        <div>
        <Button variant="light">🛒</Button>{' '}
        </div>

    )
}

export default CartWidget;